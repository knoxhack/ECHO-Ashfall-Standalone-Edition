# Ashfall Standalone Edition Module Requirements

The pack manifest declares module requirements instead of hard-coding every module file by hand.

```json
{
  "moduleArtifactFamily": "standalone",
  "moduleRequirements": [
    {
      "id": "echoashfallprotocol",
      "version": "1.0.0",
      "required": true
    }
  ]
}
```

The launcher resolves the default artifact name as `<module>-<version>-standalone.jar`. Individual requirements can override `assetName`, `path`, `sha256`, `size`, `side`, or `artifactFamily`.

## Current Published Asset Blocker

The checked-in `release-assets/v0.1.0-ashfall-standalone-edition/` files mirror the currently published GitHub prerelease assets. That published `.pack.json` snapshot does not include `moduleRequirements`, so Release Index and ECHO Launcher keep this edition warning-gated. Rebuild the `.pack.json` from `release-manifest.template.json`, regenerate checksums, upload the corrected assets, then update Release Index hashes.

<!-- runtime-parity-baseline:start -->
## Runtime Parity Baseline

Generated by `node scripts/apply-runtime-parity-pack-baseline.mjs` from the ECHO module runtime parity audit.

- Source audit: `ECHO-Modules/reports/runtime-parity/echo-module-runtime-parity-audit.json`
- Module artifact family: `standalone`
- Latest added baseline modules for `ECHO-Ashfall-Standalone-Edition`: `echohudcore`, `echoindex`, `echoholomap`, `echolens`, `echoterminal`, `echoscreencore`, `echothemecore`, `echoworldcore`

Required modules after the parity sync:

- `echoadaptercore` (1.0.0)
- `echoashfallprotocol` (1.0.0)
- `echoblockworks` (1.0.0)
- `echocore` (1.0.0)
- `echoholomap` (1.0.0)
- `echohudcore` (1.0.0)
- `echoindex` (1.0.0)
- `echolens` (1.0.0)
- `echonetcore` (1.0.0)
- `echoplatformcore` (1.0.0)
- `echoscreencore` (1.0.0)
- `echoterminal` (1.0.0)
- `echothemecore` (1.3.0)
- `echoworldcore` (1.0.0)
<!-- runtime-parity-baseline:end -->
